# VECTRA Sprint W14.5 — Single Rendering Contract

Deploy build.

Core rule:
- full Workspace is rendered only from `workspace_markdown`;
- technical API blocks are not public UI fallback;
- missing/empty `workspace_markdown` returns `workspace_generation_error`.
